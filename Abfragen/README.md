# Abfragen zum Rauskopieren

Die Datei `abfragen.sql` enthaelt alle SQL-Abfragen gesammelt.
Zusaetzlich liegen die Abfragen einzeln als separate Dateien vor:

- `01_kartenansicht_join.sql`
- `02_typverteilung_group_by.sql`
- `03_zwei_attacken_having.sql`
- `04_evolutionen_self_join.sql`
- `05_smoke_tests.sql`

Inhaltlich enthalten sind:

- Kartenansicht mit mehreren JOINs
- Typverteilung mit `GROUP BY`
- Karten mit genau zwei Attacken ueber `HAVING`
- Evolutionen per Self Join
- Smoke Tests fuer Datenqualitaet und Constraints

Die Abfragen sind fuer `information/Haupt.db` gedacht.
